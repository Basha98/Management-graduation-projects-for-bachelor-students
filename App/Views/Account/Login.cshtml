@model App.Models.loginData
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>login</title>

    <link href="~/NewFolder1/bootstrap.min.css" rel="stylesheet" />
    <link href="~/NewFolder1/Site.css" rel="stylesheet" />
    <link href="~/NewFolder1/all.css" rel="stylesheet" />
    <script src="~/NewFolder1/jquery-3.4.1.min.js"></script>
    <script src="~/NewFolder1/modernizr-2.8.3.js"></script>
    <script src="~/NewFolder1/jquery.validate.min.js"></script>
    <script src="~/NewFolder1/jquery.validate.unobtrusive.min.js"></script>
</head>
<body onload="show()">
    @using (Html.BeginForm("login", "Account", FormMethod.Post, new { @Id = "frmlogin" }))
    {
        @Html.AntiForgeryToken()
        <div class="container-fluid">
            <div class="row">

                <div class="col-xs-3"> </div>

                <div class="col-xs-6">

                    <div class=" form-group">
                        <img src="~/Resources/t2.png" />

                    </div>
                </div>


                <div class="col-xs-3"></div>

            </div>


            <div class=" row">
                <div class="col-xs-3"> </div>

                <div class="col-xs-6">
                    <label class="text-danger">@ViewBag.loginError</label>
                    <div class="login">
                        <div class="form-group">
                            <label> Email</label>
                            @Html.TextBoxFor(m => m.Email,new { @class = "form-control" })
                        </div>

                        <div class="form-group">
                            <label> Password</label>
                            @Html.PasswordFor(m => m.Password,new { @class = "form-control" })
                        </div>

                        <div class="form-group">
                            @Html.CheckBoxFor(m => m.RemmmberMe)
                            <span>RemmberMe</span>
                        </div>

                        <div class="form-group">
                            <input type="submit" value="Login" class="btn btn-primary" />
                        </div>

                    </div>

                </div>


                <div class="col-xs-3"></div>
            </div>

            <div class="row">

                <div class="col-xs-2"></div>
                <div class="col-xs-4">
                    <div>
                        @Html.ActionLink("To Back", "start", "Account", new { @class = "btn btn-danger" })
                    </div>
                </div>

                <div class="col-xs-6"></div>

            </div>
        </div>
    }

</body>
</html>
